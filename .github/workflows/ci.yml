# name: CI

# on:
#   push:
#     branches: [main]
#   pull_request:
#     branches: [main]

# jobs:
#   code-check:
#     runs-on: ubuntu-latest

#     steps:
#     - name: Checkout source code
#       uses: actions/checkout@v3

#     - name: Set up Python
#       uses: actions/setup-python@v4
#       with:
#         python-version: '3.10'

#     - name: Install dependencies
#       run: |
#         pip install -r requirements.txt

#     - name: Lint check with flake8
#       run: |
#         pip install flake8
#         flake8 .

#     - name: Run basic unit tests (optional)
#       run: |
#         pytest tests/

#     - name: Docker login
#       run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

#     - name: Pull important docker images
#       run: |
#         docker pull yourdockerhub/mlflow-server:latest
#         docker pull yourdockerhub/my-api:latest
